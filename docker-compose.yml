services:
  ig-publisher:
    image: fhir-ig-publisher:test
    build:
      context: .
      dockerfile: Dockerfile.ig-publisher
    volumes:
      - ./test-ig:/usr/src/ig
      - fhir-package-cache:/home/igpublisher/.fhir

volumes:
  fhir-package-cache:
